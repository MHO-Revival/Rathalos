// <auto-generated />
// This file was generated from XML metalib definition
// Do not modify this file manually as it will be overwritten
using System;
using System.Collections.Generic;
using Rathalos.Core.Utils.IO;

namespace Rathalos.Core.Protocol.Messages.Csproto
{
    public class CSMailSendResError : CSMailSendResResult
    {
		/// <summary>
		/// Error description
		/// </summary>
		public string ErrMsg { get; set; }
		public CRole[] MailRoleTo { get; set; } = Array.Empty<CRole>();
		/// <summary>
		/// Error return code
		/// </summary>
		public int[] ErrCode { get; set; } = Array.Empty<int>();

		/// <summary>
		/// Serializes this instance to the specified data writer.
		/// </summary>
		/// <param name="writer">The data writer to serialize to.</param>
		public void Serialize(IDataWriter writer)
		{
			if (ErrMsg.Length != CsprotoConstants.CS_MAIL_ERROR_STRING)
			{
				throw new InvalidOperationException($"Array length of 'ErrMsg' should be of length of {CsprotoConstants.CS_MAIL_ERROR_STRING} but was {ErrMsg.Length}.");
			}

			writer.WriteUTF(ErrMsg);
			// Write length for array: MailRoleTo
			var RoleCount = (int)(MailRoleTo?.Length ?? 0);
			if (RoleCount > CsprotoConstants.CS_MAIL_BUDDY_MAX_NUM)
			{
				throw new InvalidOperationException($"Array length of 'MailRoleTo' exceeds maximum allowed length of {CsprotoConstants.CS_MAIL_BUDDY_MAX_NUM} but was {RoleCount}.");
			}

			writer.WriteInt(RoleCount);
			for (var i = 0; i < RoleCount; i++)
			{
				MailRoleTo[i]?.Serialize(writer);
			}
			// Write length for array: ErrCode
			var ErrCount = (int)(ErrCode?.Length ?? 0);
			if (ErrCount > CsprotoConstants.CS_MAIL_BUDDY_MAX_NUM)
			{
				throw new InvalidOperationException($"Array length of 'ErrCode' exceeds maximum allowed length of {CsprotoConstants.CS_MAIL_BUDDY_MAX_NUM} but was {ErrCount}.");
			}

			writer.WriteInt(ErrCount);
			for (var i = 0; i < ErrCount; i++)
			{
				writer.WriteInt(ErrCode[i]);
			}
		}

		/// <summary>
		/// Deserializes this instance from the specified data reader.
		/// </summary>
		/// <param name="reader">The data reader to deserialize from.</param>
		public void Deserialize(IDataReader reader)
		{
			ErrMsg = reader.ReadUTF();
			// Read length for array: MailRoleTo
			var RoleCount = reader.ReadInt();
			// Read array: MailRoleTo
			MailRoleTo = new CRole[Math.Min((int)RoleCount, CsprotoConstants.CS_MAIL_BUDDY_MAX_NUM)];
			for (var i = 0; i < MailRoleTo.Length; i++)
			{
				MailRoleTo[i] = new CRole();
				MailRoleTo[i].Deserialize(reader);
			}
			// Read length for array: ErrCode
			var ErrCount = reader.ReadInt();
			// Read array: ErrCode
			ErrCode = new int[Math.Min((int)ErrCount, CsprotoConstants.CS_MAIL_BUDDY_MAX_NUM)];
			for (var i = 0; i < ErrCode.Length; i++)
			{
				ErrCode[i] = reader.ReadInt();
			}
		}

    }
}