// <auto-generated />
// This file was generated from XML metalib definition
// Do not modify this file manually as it will be overwritten
using System;
using System.Collections.Generic;
using Rathalos.Core.Utils.IO;

namespace Rathalos.Core.Protocol.Messages.Csproto
{
    /// <summary>
    /// Player initialize info
    /// </summary>
    public class CSPlayerInitInfo : CSPkgBody
    {
		public const CS_CMD_ID ProtocolIdConst = CS_CMD_ID.CS_CMD_PLAYER_INIT_NTF;
		/// <summary>
		/// Protocol identifier for this message type
		/// </summary>
		public ushort ProtocolId => (ushort)ProtocolIdConst;
		/// <summary>
		/// Player QQ number
		/// </summary>
		public uint AccountID { get; set; }
		/// <summary>
		/// logic entity id
		/// </summary>
		public int NetID { get; set; }
		/// <summary>
		/// DBID
		/// </summary>
		public ulong DBId { get; set; }
		/// <summary>
		/// Session id
		/// </summary>
		public uint SessionID { get; set; }
		/// <summary>
		/// Region ID
		/// </summary>
		public uint WorldID { get; set; }
		/// <summary>
		/// Server ID
		/// </summary>
		public uint ServerID { get; set; }
		/// <summary>
		/// WorldSvrID
		/// </summary>
		public uint WorldSvrID { get; set; }
		/// <summary>
		/// Server time
		/// </summary>
		public uint ServerTime { get; set; }
		/// <summary>
		/// Whether this login is reconnection
		/// </summary>
		public uint IsReConnect { get; set; }
		/// <summary>
		/// role name
		/// </summary>
		public string Name { get; set; }
		/// <summary>
		/// gender fo role
		/// </summary>
		public byte Gender { get; set; }
		/// <summary>
		/// Whether is GM
		/// </summary>
		public byte IsGM { get; set; }
		/// <summary>
		/// Appear location
		/// </summary>
		public CSQuatT Pose { get; set; }
		/// <summary>
		/// parent entityGUID
		/// </summary>
		public ulong ParentEntityGUID { get; set; }
		/// <summary>
		/// Avatar Set
		/// </summary>
		public byte AvatarSetID { get; set; }
		/// <summary>
		/// Faction
		/// </summary>
		public int Faction { get; set; }
		/// <summary>
		/// Random seed
		/// </summary>
		public uint RandSeed { get; set; }
		public int Weapon { get; set; }
		/// <summary>
		/// Last login time
		/// </summary>
		public uint LastLoginTime { get; set; }
		/// <summary>
		/// Role creation time
		/// </summary>
		public uint CreateTime { get; set; }
		/// <summary>
		/// Storage size
		/// </summary>
		public ushort StoreSize { get; set; }
		/// <summary>
		/// Normal bag capacity size
		/// </summary>
		public ushort NormalSize { get; set; }
		/// <summary>
		/// Material storage capacity size
		/// </summary>
		public ushort MaterialStoreSize { get; set; }
		/// <summary>
		/// Bag item data
		/// </summary>
		public byte[] BagItem { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Equipment data
		/// </summary>
		public byte[] EquipItem { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Storage data
		/// </summary>
		public byte[] StoreItem { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Shortcut bar data
		/// </summary>
		public CSShortcut[] Shortcut { get; set; } = Array.Empty<CSShortcut>();
		/// <summary>
		/// Buff data
		/// </summary>
		public byte[] Buff { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Skill data
		/// </summary>
		public byte[] Skill { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Pet data
		/// </summary>
		public byte[] Pet { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Friend list
		/// </summary>
		public FriendInfoPacket[] FriendData { get; set; } = Array.Empty<FriendInfoPacket>();
		/// <summary>
		/// Passerby list
		/// </summary>
		public PasserbyInfoPacket[] PasserbyData { get; set; } = Array.Empty<PasserbyInfoPacket>();
		/// <summary>
		/// Blacklist
		/// </summary>
		public BlacklistInfoPacket[] BlacklistData { get; set; } = Array.Empty<BlacklistInfoPacket>();
		/// <summary>
		/// Friend group list
		/// </summary>
		public FriendGroupPacket[] FriendGroupData { get; set; } = Array.Empty<FriendGroupPacket>();
		/// <summary>
		/// Attr data
		/// </summary>
		public byte[] Attr { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Task data
		/// </summary>
		public byte[] Task { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Guild data
		/// </summary>
		public byte[] Guild { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Action point data
		/// </summary>
		public CSActionPointData ActionPoint { get; set; }
		/// <summary>
		/// Whether first time entering level
		/// </summary>
		public int FirstEnterLevel { get; set; }
		/// <summary>
		/// Whether first time entering map
		/// </summary>
		public int FirstEnterMap { get; set; }
		/// <summary>
		/// PVP initial prepare stage state, 1 means ended, 0 means started
		/// </summary>
		public int PvpPrepareStageState { get; set; }
		/// <summary>
		/// Newbie guide completed steps
		/// </summary>
		public CSGuideStep[] GuideSteps { get; set; } = Array.Empty<CSGuideStep>();
		/// <summary>
		/// CD data
		/// </summary>
		public byte[] CD { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Schedule prize info data
		/// </summary>
		public byte[] SchedulePrize { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Mail data
		/// </summary>
		public byte[] MailInfo { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// NPC group affection data
		/// </summary>
		public byte[] NpcAtdInfo { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Current player used cat cart count
		/// </summary>
		public int CurPlayerUsedCatCarCount { get; set; }
		/// <summary>
		/// Remaining cat cuisine ID
		/// </summary>
		public int CatCuisineID { get; set; }
		/// <summary>
		/// Remaining cat cuisine level
		/// </summary>
		public byte CatCuisineLevel { get; set; }
		/// <summary>
		/// Remaining cat cuisine effects
		/// </summary>
		public byte CatCuisineBuffs { get; set; }
		/// <summary>
		/// Last cat cuisine timestamp
		/// </summary>
		public uint CatCuisineLastTm { get; set; }
		/// <summary>
		/// Cat cuisine recipe unlock count
		/// </summary>
		public int CatCuisineFormulaCount { get; set; }
		public CatCuisineDataInfo[] CatCuisineFormulaData { get; set; } = Array.Empty<CatCuisineDataInfo>();
		/// <summary>
		/// Unique use item list
		/// </summary>
		public ushort[] ItemUseOnceList { get; set; } = Array.Empty<ushort>();
		/// <summary>
		/// Hunter star level data
		/// </summary>
		public byte[] Star { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Video viewing data
		/// </summary>
		public byte[] Video { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Client miscellaneous data
		/// </summary>
		public CSClientSettings ClientSettings { get; set; }
		/// <summary>
		/// Farm data
		/// </summary>
		public byte[] Farm { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Face customization data set
		/// </summary>
		public short[] FacialInfo { get; set; } = Array.Empty<short>();
		/// <summary>
		/// Hunter path data
		/// </summary>
		public byte[] Spoor { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Rapid hunt data
		/// </summary>
		public byte[] RapidHunt { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Activity data
		/// </summary>
		public byte[] Activity { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Whether spectating
		/// </summary>
		public byte IsSpectating { get; set; }
		/// <summary>
		/// ItemRebuild data
		/// </summary>
		public byte[] ItemRebuild { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// ItemBox data
		/// </summary>
		public byte[] ItemBox { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Shop data
		/// </summary>
		public byte[] Shop { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Equipment plan length
		/// </summary>
		public byte[] EquipPlanData { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Tracking data
		/// </summary>
		public byte[] Trace { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Star stone info
		/// </summary>
		public CSStarStoneInfo StarStone { get; set; }
		/// <summary>
		/// Speak data
		/// </summary>
		public byte[] Speak { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Instance item use count data
		/// </summary>
		public byte[] BattleItemUse { get; set; } = Array.Empty<byte>();
		public byte[] SuitSkillData { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Astrolabe data
		/// </summary>
		public byte[] Astrolabe { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Wild hunt data
		/// </summary>
		public byte[] WildHunt { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Soul stone data
		/// </summary>
		public byte[] SoulStone { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Monopoly data
		/// </summary>
		public byte[] Monolopy { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Achievement data
		/// </summary>
		public byte[] Achieve { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// UI custom data info
		/// </summary>
		public ClientUIOption UIOptionInfo { get; set; }
		/// <summary>
		/// Monster illustration data
		/// </summary>
		public byte[] Illustrate { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Weapon style info
		/// </summary>
		public S2CWeaponStyleInfo WeaponStyleInfo { get; set; }
		/// <summary>
		/// Weapon acquired bit info
		/// </summary>
		public char[] WeaponHavenInfo { get; set; } = Array.Empty<char>();
		/// <summary>
		/// Silver storage box info
		/// </summary>
		public S2CSilverStorageBoxInfo SilverStorageBoxInfo { get; set; }
		/// <summary>
		/// Guide book data length
		/// </summary>
		public byte[] GuideBookData { get; set; } = Array.Empty<byte>();
		/// <summary>
		/// Secret research lab data
		/// </summary>
		public S2CSecretResearchLabDataSynchronizationRsp SecretResearchInitData { get; set; }
		/// <summary>
		/// Small shop attached DragonboxID
		/// </summary>
		public int DragonShopBox { get; set; }

		/// <summary>
		/// Serializes this instance to the specified data writer.
		/// </summary>
		/// <param name="writer">The data writer to serialize to.</param>
		public void Serialize(IDataWriter writer)
		{
			writer.WriteUInt(AccountID);
			writer.WriteInt(NetID);
			writer.WriteULong(DBId);
			writer.WriteUInt(SessionID);
			writer.WriteUInt(WorldID);
			writer.WriteUInt(ServerID);
			writer.WriteUInt(WorldSvrID);
			writer.WriteUInt(ServerTime);
			writer.WriteUInt(IsReConnect);
			if (Name.Length != CsprotoConstants.CS_MAX_ROLE_NAME)
			{
				throw new InvalidOperationException($"Array length of 'Name' should be of length of {CsprotoConstants.CS_MAX_ROLE_NAME} but was {Name.Length}.");
			}

			writer.WriteUTF(Name);
			writer.WriteByte(Gender);
			writer.WriteByte(IsGM);
			Pose?.Serialize(writer);
			writer.WriteULong(ParentEntityGUID);
			writer.WriteByte(AvatarSetID);
			writer.WriteInt(Faction);
			writer.WriteUInt(RandSeed);
			writer.WriteInt(Weapon);
			writer.WriteUInt(LastLoginTime);
			writer.WriteUInt(CreateTime);
			writer.WriteUShort(StoreSize);
			writer.WriteUShort(NormalSize);
			writer.WriteUShort(MaterialStoreSize);
			// Write length for array: BagItem
			var BagSize = (int)(BagItem?.Length ?? 0);
			if (BagSize > CsprotoConstants.CS_ITEM_BAG_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'BagItem' exceeds maximum allowed length of {CsprotoConstants.CS_ITEM_BAG_DATA_LEN} but was {BagSize}.");
			}

			writer.WriteInt(BagSize);
			for (var i = 0; i < BagSize; i++)
			{
				writer.WriteByte(BagItem[i]);
			}
			// Write length for array: EquipItem
			var EquipSize = (ushort)(EquipItem?.Length ?? 0);
			if (EquipSize > CsprotoConstants.CS_ITEM_EQUIP_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'EquipItem' exceeds maximum allowed length of {CsprotoConstants.CS_ITEM_EQUIP_DATA_LEN} but was {EquipSize}.");
			}

			writer.WriteUShort(EquipSize);
			for (var i = 0; i < EquipSize; i++)
			{
				writer.WriteByte(EquipItem[i]);
			}
			// Write length for array: StoreItem
			var StoreDataSize = (int)(StoreItem?.Length ?? 0);
			if (StoreDataSize > CsprotoConstants.CS_ITEM_STORE_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'StoreItem' exceeds maximum allowed length of {CsprotoConstants.CS_ITEM_STORE_DATA_LEN} but was {StoreDataSize}.");
			}

			writer.WriteInt(StoreDataSize);
			for (var i = 0; i < StoreDataSize; i++)
			{
				writer.WriteByte(StoreItem[i]);
			}
			// Write length for array: Shortcut
			var ShortcutCount = (ushort)(Shortcut?.Length ?? 0);
			if (ShortcutCount > CsprotoConstants.CS_MAX_SHORTCUT_LEN)
			{
				throw new InvalidOperationException($"Array length of 'Shortcut' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_SHORTCUT_LEN} but was {ShortcutCount}.");
			}

			writer.WriteUShort(ShortcutCount);
			for (var i = 0; i < ShortcutCount; i++)
			{
				Shortcut[i]?.Serialize(writer);
			}
			// Write length for array: Buff
			var BuffSize = (ushort)(Buff?.Length ?? 0);
			if (BuffSize > CsprotoConstants.CS_MAX_BUFF_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'Buff' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_BUFF_DATA_LEN} but was {BuffSize}.");
			}

			writer.WriteUShort(BuffSize);
			for (var i = 0; i < BuffSize; i++)
			{
				writer.WriteByte(Buff[i]);
			}
			// Write length for array: Skill
			var SkillSize = (ushort)(Skill?.Length ?? 0);
			if (SkillSize > CsprotoConstants.CS_MAX_SKILL_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'Skill' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_SKILL_DATA_LEN} but was {SkillSize}.");
			}

			writer.WriteUShort(SkillSize);
			for (var i = 0; i < SkillSize; i++)
			{
				writer.WriteByte(Skill[i]);
			}
			// Write length for array: Pet
			var PetSize = (ushort)(Pet?.Length ?? 0);
			if (PetSize > CsprotoConstants.CS_MAX_PET_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'Pet' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_PET_DATA_LEN} but was {PetSize}.");
			}

			writer.WriteUShort(PetSize);
			for (var i = 0; i < PetSize; i++)
			{
				writer.WriteByte(Pet[i]);
			}
			// Write length for array: FriendData
			var FriendCount = (int)(FriendData?.Length ?? 0);
			if (FriendCount > CsprotoConstants.CS_FRIEND_MAX)
			{
				throw new InvalidOperationException($"Array length of 'FriendData' exceeds maximum allowed length of {CsprotoConstants.CS_FRIEND_MAX} but was {FriendCount}.");
			}

			writer.WriteInt(FriendCount);
			for (var i = 0; i < FriendCount; i++)
			{
				FriendData[i]?.Serialize(writer);
			}
			// Write length for array: PasserbyData
			var PasserbyCount = (int)(PasserbyData?.Length ?? 0);
			if (PasserbyCount > CsprotoConstants.CS_PASSERBY_MAX)
			{
				throw new InvalidOperationException($"Array length of 'PasserbyData' exceeds maximum allowed length of {CsprotoConstants.CS_PASSERBY_MAX} but was {PasserbyCount}.");
			}

			writer.WriteInt(PasserbyCount);
			for (var i = 0; i < PasserbyCount; i++)
			{
				PasserbyData[i]?.Serialize(writer);
			}
			// Write length for array: BlacklistData
			var BlacklistCount = (int)(BlacklistData?.Length ?? 0);
			if (BlacklistCount > CsprotoConstants.CS_BLACKLIST_MAX)
			{
				throw new InvalidOperationException($"Array length of 'BlacklistData' exceeds maximum allowed length of {CsprotoConstants.CS_BLACKLIST_MAX} but was {BlacklistCount}.");
			}

			writer.WriteInt(BlacklistCount);
			for (var i = 0; i < BlacklistCount; i++)
			{
				BlacklistData[i]?.Serialize(writer);
			}
			// Write length for array: FriendGroupData
			var FriendGroupCount = (int)(FriendGroupData?.Length ?? 0);
			if (FriendGroupCount > CsprotoConstants.CS_FRIENDGROUP_MAX)
			{
				throw new InvalidOperationException($"Array length of 'FriendGroupData' exceeds maximum allowed length of {CsprotoConstants.CS_FRIENDGROUP_MAX} but was {FriendGroupCount}.");
			}

			writer.WriteInt(FriendGroupCount);
			for (var i = 0; i < FriendGroupCount; i++)
			{
				FriendGroupData[i]?.Serialize(writer);
			}
			// Write length for array: Attr
			var AttrSize = (ushort)(Attr?.Length ?? 0);
			if (AttrSize > CsprotoConstants.CS_MAX_ATTR_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'Attr' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_ATTR_DATA_LEN} but was {AttrSize}.");
			}

			writer.WriteUShort(AttrSize);
			for (var i = 0; i < AttrSize; i++)
			{
				writer.WriteByte(Attr[i]);
			}
			// Write length for array: Task
			var TaskLen = (int)(Task?.Length ?? 0);
			if (TaskLen > CsprotoConstants.CS_MAX_TASK_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'Task' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_TASK_DATA_LEN} but was {TaskLen}.");
			}

			writer.WriteInt(TaskLen);
			for (var i = 0; i < TaskLen; i++)
			{
				writer.WriteByte(Task[i]);
			}
			// Write length for array: Guild
			var GuildLen = (int)(Guild?.Length ?? 0);
			if (GuildLen > CsprotoConstants.CS_MAX_GUILD_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'Guild' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_GUILD_DATA_LEN} but was {GuildLen}.");
			}

			writer.WriteInt(GuildLen);
			for (var i = 0; i < GuildLen; i++)
			{
				writer.WriteByte(Guild[i]);
			}
			ActionPoint?.Serialize(writer);
			writer.WriteInt(FirstEnterLevel);
			writer.WriteInt(FirstEnterMap);
			writer.WriteInt(PvpPrepareStageState);
			// Write length for array: GuideSteps
			var GuideStepCount = (ushort)(GuideSteps?.Length ?? 0);
			if (GuideStepCount > CsprotoConstants.CS_MAX_GUIDE_STEPS)
			{
				throw new InvalidOperationException($"Array length of 'GuideSteps' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_GUIDE_STEPS} but was {GuideStepCount}.");
			}

			writer.WriteUShort(GuideStepCount);
			for (var i = 0; i < GuideStepCount; i++)
			{
				GuideSteps[i]?.Serialize(writer);
			}
			// Write length for array: CD
			var CDSize = (ushort)(CD?.Length ?? 0);
			if (CDSize > CsprotoConstants.CS_MAX_CD_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'CD' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_CD_DATA_LEN} but was {CDSize}.");
			}

			writer.WriteUShort(CDSize);
			for (var i = 0; i < CDSize; i++)
			{
				writer.WriteByte(CD[i]);
			}
			// Write length for array: SchedulePrize
			var SchedulePrizeLen = (int)(SchedulePrize?.Length ?? 0);
			if (SchedulePrizeLen > CsprotoConstants.CS_MAX_SCHEDULEPRIZE_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'SchedulePrize' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_SCHEDULEPRIZE_DATA_LEN} but was {SchedulePrizeLen}.");
			}

			writer.WriteInt(SchedulePrizeLen);
			for (var i = 0; i < SchedulePrizeLen; i++)
			{
				writer.WriteByte(SchedulePrize[i]);
			}
			// Write length for array: MailInfo
			var MailInfoLen = (int)(MailInfo?.Length ?? 0);
			if (MailInfoLen > CsprotoConstants.CS_MAX_MAILINFO_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'MailInfo' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_MAILINFO_DATA_LEN} but was {MailInfoLen}.");
			}

			writer.WriteInt(MailInfoLen);
			for (var i = 0; i < MailInfoLen; i++)
			{
				writer.WriteByte(MailInfo[i]);
			}
			// Write length for array: NpcAtdInfo
			var NpcAtdInfoLen = (int)(NpcAtdInfo?.Length ?? 0);
			if (NpcAtdInfoLen > CsprotoConstants.CS_MAX_NPCATDINFO_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'NpcAtdInfo' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_NPCATDINFO_DATA_LEN} but was {NpcAtdInfoLen}.");
			}

			writer.WriteInt(NpcAtdInfoLen);
			for (var i = 0; i < NpcAtdInfoLen; i++)
			{
				writer.WriteByte(NpcAtdInfo[i]);
			}
			writer.WriteInt(CurPlayerUsedCatCarCount);
			writer.WriteInt(CatCuisineID);
			// Write length for array: CatCuisineFormulaData
			var CatCuisineCount = (ushort)(CatCuisineFormulaData?.Length ?? 0);
			if (CatCuisineCount > CsprotoConstants.CS_MAX_CAT_CUISINE_UNLOCK)
			{
				throw new InvalidOperationException($"Array length of 'CatCuisineFormulaData' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_CAT_CUISINE_UNLOCK} but was {CatCuisineCount}.");
			}

			writer.WriteUShort(CatCuisineCount);
			writer.WriteByte(CatCuisineLevel);
			writer.WriteByte(CatCuisineBuffs);
			writer.WriteUInt(CatCuisineLastTm);
			writer.WriteInt(CatCuisineFormulaCount);
			for (var i = 0; i < CatCuisineCount; i++)
			{
				CatCuisineFormulaData[i]?.Serialize(writer);
			}
			// Write length for array: ItemUseOnceList
			var ItemUseOnceCount = (ushort)(ItemUseOnceList?.Length ?? 0);
			if (ItemUseOnceCount > CsprotoConstants.CS_MAX_ITEM_USE_ONCE_LIST_COUNT)
			{
				throw new InvalidOperationException($"Array length of 'ItemUseOnceList' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_ITEM_USE_ONCE_LIST_COUNT} but was {ItemUseOnceCount}.");
			}

			writer.WriteUShort(ItemUseOnceCount);
			for (var i = 0; i < ItemUseOnceCount; i++)
			{
				writer.WriteUShort(ItemUseOnceList[i]);
			}
			// Write length for array: Star
			var StarLen = (int)(Star?.Length ?? 0);
			if (StarLen > CsprotoConstants.CS_MAX_STAR_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'Star' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_STAR_DATA_LEN} but was {StarLen}.");
			}

			writer.WriteInt(StarLen);
			for (var i = 0; i < StarLen; i++)
			{
				writer.WriteByte(Star[i]);
			}
			// Write length for array: Video
			var VideoLen = (ushort)(Video?.Length ?? 0);
			if (VideoLen > CsprotoConstants.CS_MAX_SKILL_VIDEO_LEN)
			{
				throw new InvalidOperationException($"Array length of 'Video' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_SKILL_VIDEO_LEN} but was {VideoLen}.");
			}

			writer.WriteUShort(VideoLen);
			for (var i = 0; i < VideoLen; i++)
			{
				writer.WriteByte(Video[i]);
			}
			ClientSettings?.Serialize(writer);
			// Write length for array: Farm
			var FarmLen = (int)(Farm?.Length ?? 0);
			if (FarmLen > CsprotoConstants.CS_ROLE_FARMER_LEN)
			{
				throw new InvalidOperationException($"Array length of 'Farm' exceeds maximum allowed length of {CsprotoConstants.CS_ROLE_FARMER_LEN} but was {FarmLen}.");
			}

			writer.WriteInt(FarmLen);
			for (var i = 0; i < FarmLen; i++)
			{
				writer.WriteByte(Farm[i]);
			}
			if ((FacialInfo?.Length ?? 0) != CsprotoConstants.CS_MAX_FACIALINFO_COUNT)
			{
				throw new InvalidOperationException($"Array length of 'FacialInfo' should be of length of {CsprotoConstants.CS_MAX_FACIALINFO_COUNT} but was {(FacialInfo?.Length ?? 0)}.");
			}

			for (var i = 0; i < CsprotoConstants.CS_MAX_FACIALINFO_COUNT; i++)
			{
				writer.WriteShort(FacialInfo[i]);
			}
			// Write length for array: Spoor
			var SpoorLen = (int)(Spoor?.Length ?? 0);
			if (SpoorLen > CsprotoConstants.CS_MAX_SPOOR_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'Spoor' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_SPOOR_DATA_LEN} but was {SpoorLen}.");
			}

			writer.WriteInt(SpoorLen);
			for (var i = 0; i < SpoorLen; i++)
			{
				writer.WriteByte(Spoor[i]);
			}
			// Write length for array: RapidHunt
			var RapidHuntLen = (int)(RapidHunt?.Length ?? 0);
			if (RapidHuntLen > CsprotoConstants.CS_MAX_RAPIDHUNT_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'RapidHunt' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_RAPIDHUNT_DATA_LEN} but was {RapidHuntLen}.");
			}

			writer.WriteInt(RapidHuntLen);
			for (var i = 0; i < RapidHuntLen; i++)
			{
				writer.WriteByte(RapidHunt[i]);
			}
			// Write length for array: Activity
			var ActivityLen = (int)(Activity?.Length ?? 0);
			if (ActivityLen > CsprotoConstants.CS_MAX_ACTIVITY_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'Activity' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_ACTIVITY_DATA_LEN} but was {ActivityLen}.");
			}

			writer.WriteInt(ActivityLen);
			for (var i = 0; i < ActivityLen; i++)
			{
				writer.WriteByte(Activity[i]);
			}
			writer.WriteByte(IsSpectating);
			// Write length for array: ItemRebuild
			var ItemRebuildLen = (int)(ItemRebuild?.Length ?? 0);
			if (ItemRebuildLen > CsprotoConstants.CS_MAX_ITEMREBUILD_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'ItemRebuild' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_ITEMREBUILD_DATA_LEN} but was {ItemRebuildLen}.");
			}

			writer.WriteInt(ItemRebuildLen);
			for (var i = 0; i < ItemRebuildLen; i++)
			{
				writer.WriteByte(ItemRebuild[i]);
			}
			// Write length for array: ItemBox
			var ItemBoxLen = (int)(ItemBox?.Length ?? 0);
			if (ItemBoxLen > CsprotoConstants.CS_MAX_ITEMBOX_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'ItemBox' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_ITEMBOX_DATA_LEN} but was {ItemBoxLen}.");
			}

			writer.WriteInt(ItemBoxLen);
			for (var i = 0; i < ItemBoxLen; i++)
			{
				writer.WriteByte(ItemBox[i]);
			}
			// Write length for array: Shop
			var ShopLen = (int)(Shop?.Length ?? 0);
			if (ShopLen > CsprotoConstants.CS_MAX_SHOP_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'Shop' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_SHOP_DATA_LEN} but was {ShopLen}.");
			}

			writer.WriteInt(ShopLen);
			for (var i = 0; i < ShopLen; i++)
			{
				writer.WriteByte(Shop[i]);
			}
			// Write length for array: EquipPlanData
			var EquipPlanLen = (ushort)(EquipPlanData?.Length ?? 0);
			if (EquipPlanLen > CsprotoConstants.CS_MAX_EQUIP_PLAN_LEN)
			{
				throw new InvalidOperationException($"Array length of 'EquipPlanData' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_EQUIP_PLAN_LEN} but was {EquipPlanLen}.");
			}

			writer.WriteUShort(EquipPlanLen);
			for (var i = 0; i < EquipPlanLen; i++)
			{
				writer.WriteByte(EquipPlanData[i]);
			}
			// Write length for array: Trace
			var TraceLen = (int)(Trace?.Length ?? 0);
			if (TraceLen > CsprotoConstants.CS_MAX_TRACE_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'Trace' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_TRACE_DATA_LEN} but was {TraceLen}.");
			}

			writer.WriteInt(TraceLen);
			for (var i = 0; i < TraceLen; i++)
			{
				writer.WriteByte(Trace[i]);
			}
			StarStone?.Serialize(writer);
			// Write length for array: Speak
			var SpeakLen = (int)(Speak?.Length ?? 0);
			if (SpeakLen > CsprotoConstants.CS_MAX_SPEAK_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'Speak' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_SPEAK_DATA_LEN} but was {SpeakLen}.");
			}

			writer.WriteInt(SpeakLen);
			for (var i = 0; i < SpeakLen; i++)
			{
				writer.WriteByte(Speak[i]);
			}
			// Write length for array: BattleItemUse
			var BattleItemUseLen = (uint)(BattleItemUse?.Length ?? 0);
			if (BattleItemUseLen > CsprotoConstants.CS_MAX_BATTLE_ITEM_USE_LEN)
			{
				throw new InvalidOperationException($"Array length of 'BattleItemUse' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_BATTLE_ITEM_USE_LEN} but was {BattleItemUseLen}.");
			}

			writer.WriteUInt(BattleItemUseLen);
			for (var i = 0; i < BattleItemUseLen; i++)
			{
				writer.WriteByte(BattleItemUse[i]);
			}
			// Write length for array: SuitSkillData
			var SuitSkillLen = (uint)(SuitSkillData?.Length ?? 0);
			if (SuitSkillLen > CsprotoConstants.CS_SUIT_EQUIPSKIKLL_LEN)
			{
				throw new InvalidOperationException($"Array length of 'SuitSkillData' exceeds maximum allowed length of {CsprotoConstants.CS_SUIT_EQUIPSKIKLL_LEN} but was {SuitSkillLen}.");
			}

			writer.WriteUInt(SuitSkillLen);
			for (var i = 0; i < SuitSkillLen; i++)
			{
				writer.WriteByte(SuitSkillData[i]);
			}
			// Write length for array: Astrolabe
			var AstrolabeLen = (int)(Astrolabe?.Length ?? 0);
			if (AstrolabeLen > CsprotoConstants.CS_MAX_ASTROLABE_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'Astrolabe' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_ASTROLABE_DATA_LEN} but was {AstrolabeLen}.");
			}

			writer.WriteInt(AstrolabeLen);
			for (var i = 0; i < AstrolabeLen; i++)
			{
				writer.WriteByte(Astrolabe[i]);
			}
			// Write length for array: WildHunt
			var WildHuntLen = (int)(WildHunt?.Length ?? 0);
			if (WildHuntLen > CsprotoConstants.CS_MAX_WILDHUNT_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'WildHunt' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_WILDHUNT_DATA_LEN} but was {WildHuntLen}.");
			}

			writer.WriteInt(WildHuntLen);
			for (var i = 0; i < WildHuntLen; i++)
			{
				writer.WriteByte(WildHunt[i]);
			}
			// Write length for array: SoulStone
			var SoulStoneLen = (int)(SoulStone?.Length ?? 0);
			if (SoulStoneLen > CsprotoConstants.CS_MAX_SOULSTONE_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'SoulStone' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_SOULSTONE_DATA_LEN} but was {SoulStoneLen}.");
			}

			writer.WriteInt(SoulStoneLen);
			for (var i = 0; i < SoulStoneLen; i++)
			{
				writer.WriteByte(SoulStone[i]);
			}
			// Write length for array: Monolopy
			var MonolopyLen = (int)(Monolopy?.Length ?? 0);
			if (MonolopyLen > CsprotoConstants.CS_MAX_MONOLOPY_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'Monolopy' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_MONOLOPY_DATA_LEN} but was {MonolopyLen}.");
			}

			writer.WriteInt(MonolopyLen);
			for (var i = 0; i < MonolopyLen; i++)
			{
				writer.WriteByte(Monolopy[i]);
			}
			// Write length for array: Achieve
			var AchieveLen = (int)(Achieve?.Length ?? 0);
			if (AchieveLen > CsprotoConstants.CS_MAX_ACHIEVE_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'Achieve' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_ACHIEVE_DATA_LEN} but was {AchieveLen}.");
			}

			writer.WriteInt(AchieveLen);
			for (var i = 0; i < AchieveLen; i++)
			{
				writer.WriteByte(Achieve[i]);
			}
			UIOptionInfo?.Serialize(writer);
			// Write length for array: Illustrate
			var IllustrateLen = (int)(Illustrate?.Length ?? 0);
			if (IllustrateLen > CsprotoConstants.CS_MAX_ILLUSTRATE_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'Illustrate' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_ILLUSTRATE_DATA_LEN} but was {IllustrateLen}.");
			}

			writer.WriteInt(IllustrateLen);
			for (var i = 0; i < IllustrateLen; i++)
			{
				writer.WriteByte(Illustrate[i]);
			}
			WeaponStyleInfo?.Serialize(writer);
			// Write length for array: WeaponHavenInfo
			var WeaponHavenInfoCount = (int)(WeaponHavenInfo?.Length ?? 0);
			if (WeaponHavenInfoCount > CsprotoConstants.MAX_WEAPON_HAVEN_CNT)
			{
				throw new InvalidOperationException($"Array length of 'WeaponHavenInfo' exceeds maximum allowed length of {CsprotoConstants.MAX_WEAPON_HAVEN_CNT} but was {WeaponHavenInfoCount}.");
			}

			writer.WriteInt(WeaponHavenInfoCount);
			for (var i = 0; i < WeaponHavenInfoCount; i++)
			{
				writer.WriteChar(WeaponHavenInfo[i]);
			}
			SilverStorageBoxInfo?.Serialize(writer);
			// Write length for array: GuideBookData
			var GuideBookDataLen = (int)(GuideBookData?.Length ?? 0);
			if (GuideBookDataLen > CsprotoConstants.CS_MAX_GUIDE_BOOK_DATA_LEN)
			{
				throw new InvalidOperationException($"Array length of 'GuideBookData' exceeds maximum allowed length of {CsprotoConstants.CS_MAX_GUIDE_BOOK_DATA_LEN} but was {GuideBookDataLen}.");
			}

			writer.WriteInt(GuideBookDataLen);
			for (var i = 0; i < GuideBookDataLen; i++)
			{
				writer.WriteByte(GuideBookData[i]);
			}
			SecretResearchInitData?.Serialize(writer);
			writer.WriteInt(DragonShopBox);
		}

		/// <summary>
		/// Deserializes this instance from the specified data reader.
		/// </summary>
		/// <param name="reader">The data reader to deserialize from.</param>
		public void Deserialize(IDataReader reader)
		{
			AccountID = reader.ReadUInt();
			NetID = reader.ReadInt();
			DBId = reader.ReadULong();
			SessionID = reader.ReadUInt();
			WorldID = reader.ReadUInt();
			ServerID = reader.ReadUInt();
			WorldSvrID = reader.ReadUInt();
			ServerTime = reader.ReadUInt();
			IsReConnect = reader.ReadUInt();
			Name = reader.ReadUTF();
			Gender = reader.ReadByte();
			IsGM = reader.ReadByte();
			Pose = new CSQuatT();
			Pose.Deserialize(reader);
			ParentEntityGUID = reader.ReadULong();
			AvatarSetID = reader.ReadByte();
			Faction = reader.ReadInt();
			RandSeed = reader.ReadUInt();
			Weapon = reader.ReadInt();
			LastLoginTime = reader.ReadUInt();
			CreateTime = reader.ReadUInt();
			StoreSize = reader.ReadUShort();
			NormalSize = reader.ReadUShort();
			MaterialStoreSize = reader.ReadUShort();
			// Read length for array: BagItem
			var BagSize = reader.ReadInt();
			// Read array: BagItem
			BagItem = new byte[Math.Min((int)BagSize, CsprotoConstants.CS_ITEM_BAG_DATA_LEN)];
			for (var i = 0; i < BagItem.Length; i++)
			{
				BagItem[i] = reader.ReadByte();
			}
			// Read length for array: EquipItem
			var EquipSize = reader.ReadUShort();
			// Read array: EquipItem
			EquipItem = new byte[Math.Min((int)EquipSize, CsprotoConstants.CS_ITEM_EQUIP_DATA_LEN)];
			for (var i = 0; i < EquipItem.Length; i++)
			{
				EquipItem[i] = reader.ReadByte();
			}
			// Read length for array: StoreItem
			var StoreDataSize = reader.ReadInt();
			// Read array: StoreItem
			StoreItem = new byte[Math.Min((int)StoreDataSize, CsprotoConstants.CS_ITEM_STORE_DATA_LEN)];
			for (var i = 0; i < StoreItem.Length; i++)
			{
				StoreItem[i] = reader.ReadByte();
			}
			// Read length for array: Shortcut
			var ShortcutCount = reader.ReadUShort();
			// Read array: Shortcut
			Shortcut = new CSShortcut[Math.Min((int)ShortcutCount, CsprotoConstants.CS_MAX_SHORTCUT_LEN)];
			for (var i = 0; i < Shortcut.Length; i++)
			{
				Shortcut[i] = new CSShortcut();
				Shortcut[i].Deserialize(reader);
			}
			// Read length for array: Buff
			var BuffSize = reader.ReadUShort();
			// Read array: Buff
			Buff = new byte[Math.Min((int)BuffSize, CsprotoConstants.CS_MAX_BUFF_DATA_LEN)];
			for (var i = 0; i < Buff.Length; i++)
			{
				Buff[i] = reader.ReadByte();
			}
			// Read length for array: Skill
			var SkillSize = reader.ReadUShort();
			// Read array: Skill
			Skill = new byte[Math.Min((int)SkillSize, CsprotoConstants.CS_MAX_SKILL_DATA_LEN)];
			for (var i = 0; i < Skill.Length; i++)
			{
				Skill[i] = reader.ReadByte();
			}
			// Read length for array: Pet
			var PetSize = reader.ReadUShort();
			// Read array: Pet
			Pet = new byte[Math.Min((int)PetSize, CsprotoConstants.CS_MAX_PET_DATA_LEN)];
			for (var i = 0; i < Pet.Length; i++)
			{
				Pet[i] = reader.ReadByte();
			}
			// Read length for array: FriendData
			var FriendCount = reader.ReadInt();
			// Read array: FriendData
			FriendData = new FriendInfoPacket[Math.Min((int)FriendCount, CsprotoConstants.CS_FRIEND_MAX)];
			for (var i = 0; i < FriendData.Length; i++)
			{
				FriendData[i] = new FriendInfoPacket();
				FriendData[i].Deserialize(reader);
			}
			// Read length for array: PasserbyData
			var PasserbyCount = reader.ReadInt();
			// Read array: PasserbyData
			PasserbyData = new PasserbyInfoPacket[Math.Min((int)PasserbyCount, CsprotoConstants.CS_PASSERBY_MAX)];
			for (var i = 0; i < PasserbyData.Length; i++)
			{
				PasserbyData[i] = new PasserbyInfoPacket();
				PasserbyData[i].Deserialize(reader);
			}
			// Read length for array: BlacklistData
			var BlacklistCount = reader.ReadInt();
			// Read array: BlacklistData
			BlacklistData = new BlacklistInfoPacket[Math.Min((int)BlacklistCount, CsprotoConstants.CS_BLACKLIST_MAX)];
			for (var i = 0; i < BlacklistData.Length; i++)
			{
				BlacklistData[i] = new BlacklistInfoPacket();
				BlacklistData[i].Deserialize(reader);
			}
			// Read length for array: FriendGroupData
			var FriendGroupCount = reader.ReadInt();
			// Read array: FriendGroupData
			FriendGroupData = new FriendGroupPacket[Math.Min((int)FriendGroupCount, CsprotoConstants.CS_FRIENDGROUP_MAX)];
			for (var i = 0; i < FriendGroupData.Length; i++)
			{
				FriendGroupData[i] = new FriendGroupPacket();
				FriendGroupData[i].Deserialize(reader);
			}
			// Read length for array: Attr
			var AttrSize = reader.ReadUShort();
			// Read array: Attr
			Attr = new byte[Math.Min((int)AttrSize, CsprotoConstants.CS_MAX_ATTR_DATA_LEN)];
			for (var i = 0; i < Attr.Length; i++)
			{
				Attr[i] = reader.ReadByte();
			}
			// Read length for array: Task
			var TaskLen = reader.ReadInt();
			// Read array: Task
			Task = new byte[Math.Min((int)TaskLen, CsprotoConstants.CS_MAX_TASK_DATA_LEN)];
			for (var i = 0; i < Task.Length; i++)
			{
				Task[i] = reader.ReadByte();
			}
			// Read length for array: Guild
			var GuildLen = reader.ReadInt();
			// Read array: Guild
			Guild = new byte[Math.Min((int)GuildLen, CsprotoConstants.CS_MAX_GUILD_DATA_LEN)];
			for (var i = 0; i < Guild.Length; i++)
			{
				Guild[i] = reader.ReadByte();
			}
			ActionPoint = new CSActionPointData();
			ActionPoint.Deserialize(reader);
			FirstEnterLevel = reader.ReadInt();
			FirstEnterMap = reader.ReadInt();
			PvpPrepareStageState = reader.ReadInt();
			// Read length for array: GuideSteps
			var GuideStepCount = reader.ReadUShort();
			// Read array: GuideSteps
			GuideSteps = new CSGuideStep[Math.Min((int)GuideStepCount, CsprotoConstants.CS_MAX_GUIDE_STEPS)];
			for (var i = 0; i < GuideSteps.Length; i++)
			{
				GuideSteps[i] = new CSGuideStep();
				GuideSteps[i].Deserialize(reader);
			}
			// Read length for array: CD
			var CDSize = reader.ReadUShort();
			// Read array: CD
			CD = new byte[Math.Min((int)CDSize, CsprotoConstants.CS_MAX_CD_DATA_LEN)];
			for (var i = 0; i < CD.Length; i++)
			{
				CD[i] = reader.ReadByte();
			}
			// Read length for array: SchedulePrize
			var SchedulePrizeLen = reader.ReadInt();
			// Read array: SchedulePrize
			SchedulePrize = new byte[Math.Min((int)SchedulePrizeLen, CsprotoConstants.CS_MAX_SCHEDULEPRIZE_DATA_LEN)];
			for (var i = 0; i < SchedulePrize.Length; i++)
			{
				SchedulePrize[i] = reader.ReadByte();
			}
			// Read length for array: MailInfo
			var MailInfoLen = reader.ReadInt();
			// Read array: MailInfo
			MailInfo = new byte[Math.Min((int)MailInfoLen, CsprotoConstants.CS_MAX_MAILINFO_DATA_LEN)];
			for (var i = 0; i < MailInfo.Length; i++)
			{
				MailInfo[i] = reader.ReadByte();
			}
			// Read length for array: NpcAtdInfo
			var NpcAtdInfoLen = reader.ReadInt();
			// Read array: NpcAtdInfo
			NpcAtdInfo = new byte[Math.Min((int)NpcAtdInfoLen, CsprotoConstants.CS_MAX_NPCATDINFO_DATA_LEN)];
			for (var i = 0; i < NpcAtdInfo.Length; i++)
			{
				NpcAtdInfo[i] = reader.ReadByte();
			}
			CurPlayerUsedCatCarCount = reader.ReadInt();
			CatCuisineID = reader.ReadInt();
			// Read length for array: CatCuisineFormulaData
			var CatCuisineCount = reader.ReadUShort();
			CatCuisineLevel = reader.ReadByte();
			CatCuisineBuffs = reader.ReadByte();
			CatCuisineLastTm = reader.ReadUInt();
			CatCuisineFormulaCount = reader.ReadInt();
			// Read array: CatCuisineFormulaData
			CatCuisineFormulaData = new CatCuisineDataInfo[Math.Min((int)CatCuisineCount, CsprotoConstants.CS_MAX_CAT_CUISINE_UNLOCK)];
			for (var i = 0; i < CatCuisineFormulaData.Length; i++)
			{
				CatCuisineFormulaData[i] = new CatCuisineDataInfo();
				CatCuisineFormulaData[i].Deserialize(reader);
			}
			// Read length for array: ItemUseOnceList
			var ItemUseOnceCount = reader.ReadUShort();
			// Read array: ItemUseOnceList
			ItemUseOnceList = new ushort[Math.Min((int)ItemUseOnceCount, CsprotoConstants.CS_MAX_ITEM_USE_ONCE_LIST_COUNT)];
			for (var i = 0; i < ItemUseOnceList.Length; i++)
			{
				ItemUseOnceList[i] = reader.ReadUShort();
			}
			// Read length for array: Star
			var StarLen = reader.ReadInt();
			// Read array: Star
			Star = new byte[Math.Min((int)StarLen, CsprotoConstants.CS_MAX_STAR_DATA_LEN)];
			for (var i = 0; i < Star.Length; i++)
			{
				Star[i] = reader.ReadByte();
			}
			// Read length for array: Video
			var VideoLen = reader.ReadUShort();
			// Read array: Video
			Video = new byte[Math.Min((int)VideoLen, CsprotoConstants.CS_MAX_SKILL_VIDEO_LEN)];
			for (var i = 0; i < Video.Length; i++)
			{
				Video[i] = reader.ReadByte();
			}
			ClientSettings = new CSClientSettings();
			ClientSettings.Deserialize(reader);
			// Read length for array: Farm
			var FarmLen = reader.ReadInt();
			// Read array: Farm
			Farm = new byte[Math.Min((int)FarmLen, CsprotoConstants.CS_ROLE_FARMER_LEN)];
			for (var i = 0; i < Farm.Length; i++)
			{
				Farm[i] = reader.ReadByte();
			}
			// Read array: FacialInfo
			FacialInfo = new short[CsprotoConstants.CS_MAX_FACIALINFO_COUNT];
			for (var i = 0; i < FacialInfo.Length; i++)
			{
				FacialInfo[i] = reader.ReadShort();
			}
			// Read length for array: Spoor
			var SpoorLen = reader.ReadInt();
			// Read array: Spoor
			Spoor = new byte[Math.Min((int)SpoorLen, CsprotoConstants.CS_MAX_SPOOR_DATA_LEN)];
			for (var i = 0; i < Spoor.Length; i++)
			{
				Spoor[i] = reader.ReadByte();
			}
			// Read length for array: RapidHunt
			var RapidHuntLen = reader.ReadInt();
			// Read array: RapidHunt
			RapidHunt = new byte[Math.Min((int)RapidHuntLen, CsprotoConstants.CS_MAX_RAPIDHUNT_DATA_LEN)];
			for (var i = 0; i < RapidHunt.Length; i++)
			{
				RapidHunt[i] = reader.ReadByte();
			}
			// Read length for array: Activity
			var ActivityLen = reader.ReadInt();
			// Read array: Activity
			Activity = new byte[Math.Min((int)ActivityLen, CsprotoConstants.CS_MAX_ACTIVITY_DATA_LEN)];
			for (var i = 0; i < Activity.Length; i++)
			{
				Activity[i] = reader.ReadByte();
			}
			IsSpectating = reader.ReadByte();
			// Read length for array: ItemRebuild
			var ItemRebuildLen = reader.ReadInt();
			// Read array: ItemRebuild
			ItemRebuild = new byte[Math.Min((int)ItemRebuildLen, CsprotoConstants.CS_MAX_ITEMREBUILD_DATA_LEN)];
			for (var i = 0; i < ItemRebuild.Length; i++)
			{
				ItemRebuild[i] = reader.ReadByte();
			}
			// Read length for array: ItemBox
			var ItemBoxLen = reader.ReadInt();
			// Read array: ItemBox
			ItemBox = new byte[Math.Min((int)ItemBoxLen, CsprotoConstants.CS_MAX_ITEMBOX_DATA_LEN)];
			for (var i = 0; i < ItemBox.Length; i++)
			{
				ItemBox[i] = reader.ReadByte();
			}
			// Read length for array: Shop
			var ShopLen = reader.ReadInt();
			// Read array: Shop
			Shop = new byte[Math.Min((int)ShopLen, CsprotoConstants.CS_MAX_SHOP_DATA_LEN)];
			for (var i = 0; i < Shop.Length; i++)
			{
				Shop[i] = reader.ReadByte();
			}
			// Read length for array: EquipPlanData
			var EquipPlanLen = reader.ReadUShort();
			// Read array: EquipPlanData
			EquipPlanData = new byte[Math.Min((int)EquipPlanLen, CsprotoConstants.CS_MAX_EQUIP_PLAN_LEN)];
			for (var i = 0; i < EquipPlanData.Length; i++)
			{
				EquipPlanData[i] = reader.ReadByte();
			}
			// Read length for array: Trace
			var TraceLen = reader.ReadInt();
			// Read array: Trace
			Trace = new byte[Math.Min((int)TraceLen, CsprotoConstants.CS_MAX_TRACE_DATA_LEN)];
			for (var i = 0; i < Trace.Length; i++)
			{
				Trace[i] = reader.ReadByte();
			}
			StarStone = new CSStarStoneInfo();
			StarStone.Deserialize(reader);
			// Read length for array: Speak
			var SpeakLen = reader.ReadInt();
			// Read array: Speak
			Speak = new byte[Math.Min((int)SpeakLen, CsprotoConstants.CS_MAX_SPEAK_DATA_LEN)];
			for (var i = 0; i < Speak.Length; i++)
			{
				Speak[i] = reader.ReadByte();
			}
			// Read length for array: BattleItemUse
			var BattleItemUseLen = reader.ReadUInt();
			// Read array: BattleItemUse
			BattleItemUse = new byte[Math.Min((int)BattleItemUseLen, CsprotoConstants.CS_MAX_BATTLE_ITEM_USE_LEN)];
			for (var i = 0; i < BattleItemUse.Length; i++)
			{
				BattleItemUse[i] = reader.ReadByte();
			}
			// Read length for array: SuitSkillData
			var SuitSkillLen = reader.ReadUInt();
			// Read array: SuitSkillData
			SuitSkillData = new byte[Math.Min((int)SuitSkillLen, CsprotoConstants.CS_SUIT_EQUIPSKIKLL_LEN)];
			for (var i = 0; i < SuitSkillData.Length; i++)
			{
				SuitSkillData[i] = reader.ReadByte();
			}
			// Read length for array: Astrolabe
			var AstrolabeLen = reader.ReadInt();
			// Read array: Astrolabe
			Astrolabe = new byte[Math.Min((int)AstrolabeLen, CsprotoConstants.CS_MAX_ASTROLABE_DATA_LEN)];
			for (var i = 0; i < Astrolabe.Length; i++)
			{
				Astrolabe[i] = reader.ReadByte();
			}
			// Read length for array: WildHunt
			var WildHuntLen = reader.ReadInt();
			// Read array: WildHunt
			WildHunt = new byte[Math.Min((int)WildHuntLen, CsprotoConstants.CS_MAX_WILDHUNT_DATA_LEN)];
			for (var i = 0; i < WildHunt.Length; i++)
			{
				WildHunt[i] = reader.ReadByte();
			}
			// Read length for array: SoulStone
			var SoulStoneLen = reader.ReadInt();
			// Read array: SoulStone
			SoulStone = new byte[Math.Min((int)SoulStoneLen, CsprotoConstants.CS_MAX_SOULSTONE_DATA_LEN)];
			for (var i = 0; i < SoulStone.Length; i++)
			{
				SoulStone[i] = reader.ReadByte();
			}
			// Read length for array: Monolopy
			var MonolopyLen = reader.ReadInt();
			// Read array: Monolopy
			Monolopy = new byte[Math.Min((int)MonolopyLen, CsprotoConstants.CS_MAX_MONOLOPY_DATA_LEN)];
			for (var i = 0; i < Monolopy.Length; i++)
			{
				Monolopy[i] = reader.ReadByte();
			}
			// Read length for array: Achieve
			var AchieveLen = reader.ReadInt();
			// Read array: Achieve
			Achieve = new byte[Math.Min((int)AchieveLen, CsprotoConstants.CS_MAX_ACHIEVE_DATA_LEN)];
			for (var i = 0; i < Achieve.Length; i++)
			{
				Achieve[i] = reader.ReadByte();
			}
			UIOptionInfo = new ClientUIOption();
			UIOptionInfo.Deserialize(reader);
			// Read length for array: Illustrate
			var IllustrateLen = reader.ReadInt();
			// Read array: Illustrate
			Illustrate = new byte[Math.Min((int)IllustrateLen, CsprotoConstants.CS_MAX_ILLUSTRATE_DATA_LEN)];
			for (var i = 0; i < Illustrate.Length; i++)
			{
				Illustrate[i] = reader.ReadByte();
			}
			WeaponStyleInfo = new S2CWeaponStyleInfo();
			WeaponStyleInfo.Deserialize(reader);
			// Read length for array: WeaponHavenInfo
			var WeaponHavenInfoCount = reader.ReadInt();
			// Read array: WeaponHavenInfo
			WeaponHavenInfo = new char[Math.Min((int)WeaponHavenInfoCount, CsprotoConstants.MAX_WEAPON_HAVEN_CNT)];
			for (var i = 0; i < WeaponHavenInfo.Length; i++)
			{
				WeaponHavenInfo[i] = reader.ReadChar();
			}
			SilverStorageBoxInfo = new S2CSilverStorageBoxInfo();
			SilverStorageBoxInfo.Deserialize(reader);
			// Read length for array: GuideBookData
			var GuideBookDataLen = reader.ReadInt();
			// Read array: GuideBookData
			GuideBookData = new byte[Math.Min((int)GuideBookDataLen, CsprotoConstants.CS_MAX_GUIDE_BOOK_DATA_LEN)];
			for (var i = 0; i < GuideBookData.Length; i++)
			{
				GuideBookData[i] = reader.ReadByte();
			}
			SecretResearchInitData = new S2CSecretResearchLabDataSynchronizationRsp();
			SecretResearchInitData.Deserialize(reader);
			DragonShopBox = reader.ReadInt();
		}

    }
}