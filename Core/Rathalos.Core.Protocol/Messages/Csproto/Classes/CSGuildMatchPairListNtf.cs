// <auto-generated />
// This file was generated from XML metalib definition
// Do not modify this file manually as it will be overwritten
using System;
using System.Collections.Generic;
using Rathalos.Core.Utils.IO;

namespace Rathalos.Core.Protocol.Messages.Csproto
{
    /// <summary>
    /// Guild match knockout round pairing list
    /// </summary>
    public class CSGuildMatchPairListNtf : CSPkgBody
    {
		public const CS_CMD_ID ProtocolIdConst = CS_CMD_ID.CS_CMD_GUILD_MATCH_PAIR_LIST_NTF;
		/// <summary>
		/// Protocol identifier for this message type
		/// </summary>
		public int ProtocolId => (int)ProtocolIdConst;
		public uint[] SignUp64IDs { get; set; } = Array.Empty<uint>();
		public uint[] SignUp32IDs { get; set; } = Array.Empty<uint>();
		public uint[] SignUp16IDs { get; set; } = Array.Empty<uint>();
		public uint[] SignUp8IDs { get; set; } = Array.Empty<uint>();
		public uint[] SignUp4IDs { get; set; } = Array.Empty<uint>();
		public uint[] SignUp2IDs { get; set; } = Array.Empty<uint>();
		public uint WinSignID { get; set; }

		/// <summary>
		/// Serializes this instance to the specified data writer.
		/// </summary>
		/// <param name="writer">The data writer to serialize to.</param>
		public void Serialize(IDataWriter writer)
		{
			// Write length for array: SignUp64IDs
			var Count64 = (int)(SignUp64IDs?.Length ?? 0);
			if (Count64 > 64)
			{
				throw new InvalidOperationException($"Array length of 'SignUp64IDs' exceeds maximum allowed length of {64} but was {Count64}.");
			}

			writer.WriteInt(Count64);
			for (var i = 0; i < Count64; i++)
			{
				writer.WriteUInt(SignUp64IDs[i]);
			}
			// Write length for array: SignUp32IDs
			var Count32 = (int)(SignUp32IDs?.Length ?? 0);
			if (Count32 > 32)
			{
				throw new InvalidOperationException($"Array length of 'SignUp32IDs' exceeds maximum allowed length of {32} but was {Count32}.");
			}

			writer.WriteInt(Count32);
			for (var i = 0; i < Count32; i++)
			{
				writer.WriteUInt(SignUp32IDs[i]);
			}
			// Write length for array: SignUp16IDs
			var Count16 = (int)(SignUp16IDs?.Length ?? 0);
			if (Count16 > 16)
			{
				throw new InvalidOperationException($"Array length of 'SignUp16IDs' exceeds maximum allowed length of {16} but was {Count16}.");
			}

			writer.WriteInt(Count16);
			for (var i = 0; i < Count16; i++)
			{
				writer.WriteUInt(SignUp16IDs[i]);
			}
			// Write length for array: SignUp8IDs
			var Count8 = (int)(SignUp8IDs?.Length ?? 0);
			if (Count8 > 8)
			{
				throw new InvalidOperationException($"Array length of 'SignUp8IDs' exceeds maximum allowed length of {8} but was {Count8}.");
			}

			writer.WriteInt(Count8);
			for (var i = 0; i < Count8; i++)
			{
				writer.WriteUInt(SignUp8IDs[i]);
			}
			// Write length for array: SignUp4IDs
			var Count4 = (int)(SignUp4IDs?.Length ?? 0);
			if (Count4 > 4)
			{
				throw new InvalidOperationException($"Array length of 'SignUp4IDs' exceeds maximum allowed length of {4} but was {Count4}.");
			}

			writer.WriteInt(Count4);
			for (var i = 0; i < Count4; i++)
			{
				writer.WriteUInt(SignUp4IDs[i]);
			}
			// Write length for array: SignUp2IDs
			var Count2 = (int)(SignUp2IDs?.Length ?? 0);
			if (Count2 > 2)
			{
				throw new InvalidOperationException($"Array length of 'SignUp2IDs' exceeds maximum allowed length of {2} but was {Count2}.");
			}

			writer.WriteInt(Count2);
			for (var i = 0; i < Count2; i++)
			{
				writer.WriteUInt(SignUp2IDs[i]);
			}
			writer.WriteUInt(WinSignID);
		}

		/// <summary>
		/// Deserializes this instance from the specified data reader.
		/// </summary>
		/// <param name="reader">The data reader to deserialize from.</param>
		public void Deserialize(IDataReader reader)
		{
			// Read length for array: SignUp64IDs
			var Count64 = reader.ReadInt();
			// Read array: SignUp64IDs
			SignUp64IDs = new uint[Math.Min((int)Count64, 64)];
			for (var i = 0; i < SignUp64IDs.Length; i++)
			{
				SignUp64IDs[i] = reader.ReadUInt();
			}
			// Read length for array: SignUp32IDs
			var Count32 = reader.ReadInt();
			// Read array: SignUp32IDs
			SignUp32IDs = new uint[Math.Min((int)Count32, 32)];
			for (var i = 0; i < SignUp32IDs.Length; i++)
			{
				SignUp32IDs[i] = reader.ReadUInt();
			}
			// Read length for array: SignUp16IDs
			var Count16 = reader.ReadInt();
			// Read array: SignUp16IDs
			SignUp16IDs = new uint[Math.Min((int)Count16, 16)];
			for (var i = 0; i < SignUp16IDs.Length; i++)
			{
				SignUp16IDs[i] = reader.ReadUInt();
			}
			// Read length for array: SignUp8IDs
			var Count8 = reader.ReadInt();
			// Read array: SignUp8IDs
			SignUp8IDs = new uint[Math.Min((int)Count8, 8)];
			for (var i = 0; i < SignUp8IDs.Length; i++)
			{
				SignUp8IDs[i] = reader.ReadUInt();
			}
			// Read length for array: SignUp4IDs
			var Count4 = reader.ReadInt();
			// Read array: SignUp4IDs
			SignUp4IDs = new uint[Math.Min((int)Count4, 4)];
			for (var i = 0; i < SignUp4IDs.Length; i++)
			{
				SignUp4IDs[i] = reader.ReadUInt();
			}
			// Read length for array: SignUp2IDs
			var Count2 = reader.ReadInt();
			// Read array: SignUp2IDs
			SignUp2IDs = new uint[Math.Min((int)Count2, 2)];
			for (var i = 0; i < SignUp2IDs.Length; i++)
			{
				SignUp2IDs[i] = reader.ReadUInt();
			}
			WinSignID = reader.ReadUInt();
		}

    }
}