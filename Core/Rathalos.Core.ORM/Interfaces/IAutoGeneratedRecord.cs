namespace Rathalos.Core.ORM.Interfaces
{
    /// <summary>
    /// Marker interface indicating that the implementing record type
    /// should have its table automatically created at runtime if it doesn't exist.
    /// </summary>
    public interface IAutoGeneratedRecord
    {
    }
}
